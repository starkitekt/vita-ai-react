{
    "buildCommand": "cd vita-ai-react && npm install && npm run build",
    "outputDirectory": "vita-ai-react/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}